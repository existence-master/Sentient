# Extends the frozen production lockfile with optional dev / local-ML packages.
# Install: pip install -r requirements-dev.txt
#
# llama-cpp-python: not pinned here for Windows (no official wheel; needs MSVC + CMake to build).
#   On Linux/macOS dev machines, add e.g. llama-cpp-python==0.2.90 or install from your distro.

-r requirements.txt

faster-whisper==1.2.1
snac==1.2.1
